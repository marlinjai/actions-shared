name: Coolify Deploy + Verify

on:
  workflow_call:
    inputs:
      health_url:
        required: true
        type: string
      environment:
        required: true
        type: string
    secrets:
      coolify_webhook:
        required: true
      coolify_token:
        required: true

jobs:
  deploy-verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy-verify called with health_url=${{ inputs.health_url }} env=${{ inputs.environment }}"
